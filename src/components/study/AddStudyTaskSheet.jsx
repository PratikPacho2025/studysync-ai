import { X } from 'lucide-react'
import { useState } from 'react'
import { blankStudyTask, taskTypes } from '../../data/mock/studyPlan'

export function AddStudyTaskSheet({ isOpen, onClose, onAdd }) {
  const [form, setForm] = useState(blankStudyTask)
  if (!isOpen) return null
  const update = (event) => setForm((current) => ({ ...current, [event.target.name]: event.target.value }))
  const submit = (event) => { event.preventDefault(); onAdd({ ...form, id: Date.now(), duration: Number(form.duration) }); setForm(blankStudyTask) }
  return <div className="fixed inset-0 z-50 flex items-end justify-center bg-[#173e32]/35 p-0 sm:items-center sm:p-5"><div className="max-h-[92vh] w-full overflow-y-auto rounded-t-3xl bg-[#f8fbf8] p-5 shadow-2xl sm:max-w-lg sm:rounded-3xl sm:p-7"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-[var(--color-accent)]">Plan your next block</p><h2 className="mt-1 text-2xl font-semibold text-[var(--color-ink)]">Add Study Task</h2></div><button type="button" onClick={onClose} aria-label="Close add study task form" className="grid size-10 place-items-center rounded-xl text-[var(--color-muted)] hover:bg-white"><X size={19} aria-hidden="true" /></button></div><form onSubmit={submit} className="mt-6 space-y-4"><label className="block text-sm font-semibold text-[var(--color-ink)]">Task<input required name="title" value={form.title} onChange={update} className="mt-2 min-h-11 w-full rounded-xl border border-[#d8e5db] bg-white px-3 text-sm font-normal outline-none focus:border-[var(--color-accent)]" /></label><div className="grid gap-4 sm:grid-cols-2">{[['subject', 'Subject'], ['topic', 'Topic']].map(([name, label]) => <label key={name} className="block text-sm font-semibold text-[var(--color-ink)]">{label}<input required name={name} value={form[name]} onChange={update} className="mt-2 min-h-11 w-full rounded-xl border border-[#d8e5db] bg-white px-3 text-sm font-normal outline-none focus:border-[var(--color-accent)]" /></label>)}</div><div className="grid gap-4 sm:grid-cols-2"><label className="block text-sm font-semibold text-[var(--color-ink)]">Type<select name="type" value={form.type} onChange={update} className="mt-2 min-h-11 w-full rounded-xl border border-[#d8e5db] bg-white px-3 text-sm font-normal outline-none focus:border-[var(--color-accent)]">{taskTypes.map((type) => <option key={type}>{type}</option>)}</select></label><label className="block text-sm font-semibold text-[var(--color-ink)]">Duration (min)<input required min="5" type="number" name="duration" value={form.duration} onChange={update} className="mt-2 min-h-11 w-full rounded-xl border border-[#d8e5db] bg-white px-3 text-sm font-normal outline-none focus:border-[var(--color-accent)]" /></label></div><div className="grid gap-4 sm:grid-cols-2"><label className="block text-sm font-semibold text-[var(--color-ink)]">Priority<select name="priority" value={form.priority} onChange={update} className="mt-2 min-h-11 w-full rounded-xl border border-[#d8e5db] bg-white px-3 text-sm font-normal outline-none focus:border-[var(--color-accent)]"><option value="high">High</option><option value="medium">Medium</option><option value="low">Low</option></select></label><label className="block text-sm font-semibold text-[var(--color-ink)]">Due date<select name="dueDate" value={form.dueDate} onChange={update} className="mt-2 min-h-11 w-full rounded-xl border border-[#d8e5db] bg-white px-3 text-sm font-normal outline-none focus:border-[var(--color-accent)]"><option value="today">Today</option><option value="tomorrow">Tomorrow</option><option value="in-2-days">In 2 days</option></select></label></div><label className="block text-sm font-semibold text-[var(--color-ink)]">Due time<input required name="dueTime" value={form.dueTime} onChange={update} className="mt-2 min-h-11 w-full rounded-xl border border-[#d8e5db] bg-white px-3 text-sm font-normal outline-none focus:border-[var(--color-accent)]" /></label><div className="flex gap-3 pt-2"><button type="button" onClick={onClose} className="min-h-11 flex-1 rounded-xl border border-[#d8e5db] text-sm font-bold text-[var(--color-muted)]">Cancel</button><button type="submit" className="min-h-11 flex-1 rounded-xl bg-[var(--color-accent)] text-sm font-bold text-white">Add Task</button></div></form></div></div>
}