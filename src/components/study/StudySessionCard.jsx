import { Check, Pause, Play, TimerReset } from 'lucide-react'

function formatTime(seconds) { return `${String(Math.floor(seconds / 60)).padStart(2, '0')}:${String(seconds % 60).padStart(2, '0')}` }

export function StudySessionCard({ task, remainingTime, isRunning, onToggle, onComplete }) {
  return <section className="dashboard-card bg-[#1e5c4e] p-5 text-white sm:p-6" aria-labelledby="study-session-title"><div className="flex items-center gap-2 text-[#b9dfc7]"><TimerReset size={18} aria-hidden="true" /><p className="text-xs font-bold uppercase tracking-[0.18em]">Active session</p></div><h2 id="study-session-title" className="mt-3 text-xl font-semibold">{task.title}</h2><p className="mt-1 text-sm text-[#d5e9dc]">{task.topic}</p><div className="py-7 text-center"><time className="text-6xl font-semibold tracking-tight" dateTime={`PT${remainingTime}S`}>{formatTime(remainingTime)}</time><p className="mt-2 text-xs font-semibold text-[#b9dfc7]">{isRunning ? 'Session in progress' : 'Session paused'}</p></div><div className="flex gap-3"><button type="button" onClick={onToggle} className="flex min-h-11 flex-1 items-center justify-center gap-2 rounded-xl bg-white/10 text-sm font-bold hover:bg-white/20">{isRunning ? <Pause size={16} aria-hidden="true" /> : <Play size={16} fill="currentColor" aria-hidden="true" />}{isRunning ? 'Pause' : 'Resume'}</button><button type="button" onClick={onComplete} className="flex min-h-11 flex-1 items-center justify-center gap-2 rounded-xl bg-[#b9dfc7] text-sm font-bold text-[#183f3b] hover:bg-white"><Check size={16} aria-hidden="true" /> Complete Session</button></div></section>
}