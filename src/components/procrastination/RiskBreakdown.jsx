import { ProcrastinationMetric } from './ProcrastinationMetric'

export function RiskBreakdown({ data }) { return <section className="dashboard-card bg-white p-5 sm:p-6" aria-labelledby="breakdown-title"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-[var(--color-accent)]">What is shaping it</p><h2 id="breakdown-title" className="mt-1 text-xl font-semibold text-[var(--color-ink)]">Risk Breakdown</h2></div><div className="mt-5 grid grid-cols-2 gap-3">{[['Missed sessions', data.missedSessions, '3 this week', 'warning'], ['Ignored reminders', data.ignoredReminders, '2 this week', 'warning'], ['Pending topics', data.pendingTopics, 'Need a next step', 'normal'], ['Delayed revisions', data.delayedRevisions, 'In the current cycle', 'normal'], ['Exam pressure', `${data.examPressure}%`, 'Medium pressure', 'normal']].map(([label, value, detail, severity]) => <ProcrastinationMetric key={label} label={label} value={value} detail={detail} severity={severity} />)}</div></section> }