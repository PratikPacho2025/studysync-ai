import { Check, Play, X } from 'lucide-react'
import { PriorityBadge } from './PriorityBadge'

export function StudyTaskDetailsSheet({ task, onClose, onStart, onComplete }) {
  if (!task) return null
  return <div className="fixed inset-0 z-50 flex items-end justify-center bg-[#173e32]/35 p-0 sm:items-center sm:p-5"><div className="w-full rounded-t-3xl bg-[#f8fbf8] p-5 shadow-2xl sm:max-w-lg sm:rounded-3xl sm:p-7"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-[var(--color-accent)]">Task details</p><h2 className="mt-1 text-2xl font-semibold text-[var(--color-ink)]">{task.title}</h2></div><button type="button" onClick={onClose} aria-label="Close task details" className="grid size-10 place-items-center rounded-xl text-[var(--color-muted)] hover:bg-white"><X size={19} aria-hidden="true" /></button></div><dl className="mt-6 divide-y divide-[#e2ece4] rounded-2xl bg-white px-4"><div className="flex justify-between gap-4 py-3"><dt className="text-sm text-[var(--color-muted)]">Subject</dt><dd className="text-right text-sm font-semibold text-[var(--color-ink)]">{task.subject}</dd></div><div className="flex justify-between gap-4 py-3"><dt className="text-sm text-[var(--color-muted)]">Topic</dt><dd className="text-right text-sm font-semibold text-[var(--color-ink)]">{task.topic}</dd></div><div className="flex justify-between gap-4 py-3"><dt className="text-sm text-[var(--color-muted)]">Duration</dt><dd className="text-right text-sm font-semibold text-[var(--color-ink)]">{task.duration} minutes</dd></div><div className="flex justify-between gap-4 py-3"><dt className="text-sm text-[var(--color-muted)]">Priority</dt><dd><PriorityBadge priority={task.priority} /></dd></div><div className="flex justify-between gap-4 py-3"><dt className="text-sm text-[var(--color-muted)]">Due</dt><dd className="text-right text-sm font-semibold text-[var(--color-ink)]">{task.dueDate} · {task.dueTime}</dd></div></dl><div className="mt-5 flex gap-3">{task.status !== 'completed' && <button type="button" onClick={() => onStart(task)} className="flex min-h-11 flex-1 items-center justify-center gap-2 rounded-xl bg-[#e1f0e7] text-sm font-bold text-[var(--color-accent)]"><Play size={16} fill="currentColor" aria-hidden="true" /> Start Study</button>}<button type="button" onClick={() => onComplete(task.id)} className="flex min-h-11 flex-1 items-center justify-center gap-2 rounded-xl bg-[var(--color-accent)] text-sm font-bold text-white"><Check size={16} aria-hidden="true" /> Mark Complete</button></div></div></div>
}