import { Clock3, TrendingUp } from 'lucide-react'
export function StudyTimeChart({ studyTime }) { const max = Math.max(...studyTime.daily); const change = Math.round(((studyTime.totalMinutes - studyTime.previousWeekMinutes) / studyTime.previousWeekMinutes) * 100); return <section className="dashboard-card bg-white p-5 sm:p-6" aria-labelledby="study-time-title"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-[var(--color-accent)]">Time invested</p><h2 id="study-time-title" className="mt-1 text-xl font-semibold text-[var(--color-ink)]">Study Time</h2></div><Clock3 size={20} className="text-[var(--color-accent)]" aria-hidden="true" /></div><div className="mt-5 flex items-end gap-3"><strong className="text-3xl font-semibold text-[var(--color-ink)]">{Math.floor(studyTime.totalMinutes / 60)}h {studyTime.totalMinutes % 60}m</strong><span className="mb-1 flex items-center gap-1 text-xs font-bold text-[var(--color-accent)]"><TrendingUp size={13} aria-hidden="true" /> +{change}%</span></div><div className="mt-6 flex h-28 items-end gap-2">{studyTime.daily.map((minutes, index) => <div key={index} className="flex h-full flex-1 flex-col items-center justify-end gap-2"><div className="w-full rounded-t-lg bg-[#b9dfc7]" style={{ height: `${Math.max(8, (minutes / max) * 100)}%` }} title={`${minutes} minutes`} /><span className="text-[10px] font-semibold text-[var(--color-muted)]">{['M', 'T', 'W', 'T', 'F', 'S', 'S'][index]}</span></div>)}</div></section> }