import { ArrowRight, Flame, Play } from 'lucide-react'
import { PriorityBadge } from './PriorityBadge'

export function FocusNowCard({ task, onStart, onSelect }) {
  if (!task) return <section className="dashboard-card bg-[#eef5f0] p-5"><h2 className="text-xl font-semibold text-[var(--color-ink)]">Focus Now</h2><p className="mt-2 text-sm text-[var(--color-muted)]">Your study queue is clear. Nice work.</p></section>
  return <section className="dashboard-card 3d-hover overflow-hidden bg-[#183f3b] p-5 text-white sm:p-6" aria-labelledby="focus-now-title"><div className="flex items-center gap-2 text-[#b9dfc7]"><Flame size={17} aria-hidden="true" /><p className="text-xs font-bold uppercase tracking-[0.18em]">Focus Now</p></div><h2 id="focus-now-title" className="mt-4 text-2xl font-semibold">{task.title}</h2><p className="mt-2 text-sm text-[#d5e9dc]">{task.topic}</p><div className="mt-4 flex items-center gap-3"><PriorityBadge priority={task.priority} /><span className="text-xs font-semibold text-[#d5e9dc]">{task.duration} minutes</span></div><div className="mt-5 flex gap-2"><button type="button" onClick={() => onStart(task)} className="inline-flex min-h-11 items-center gap-2 rounded-xl bg-[#b9dfc7] px-4 text-sm font-bold text-[#183f3b] transition hover:bg-white active:scale-95"><Play size={16} fill="currentColor" aria-hidden="true" /> Start Study</button><button type="button" onClick={() => onSelect(task)} aria-label="View focus task details" className="grid size-11 place-items-center rounded-xl bg-white/10 text-[#d5e9dc] hover:bg-white/20"><ArrowRight size={18} aria-hidden="true" /></button></div></section>
}