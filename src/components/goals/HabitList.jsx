import { useState } from 'react'
import { CheckCircle2, Filter } from 'lucide-react'
import { DailyHabitCard } from './DailyHabitCard'
export function HabitList({ habits, goalNames, onToggle }) { const [filter, setFilter] = useState('all'); const visible = habits.filter((habit) => filter === 'all' || (filter === 'completed' ? habit.completed : !habit.completed)); return <section className="dashboard-card bg-white p-5 sm:p-6" aria-labelledby="habit-list-title"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-[var(--color-accent)]">Daily actions</p><h2 id="habit-list-title" className="mt-1 text-xl font-semibold text-[var(--color-ink)]">Today&apos;s Habits</h2></div><CheckCircle2 size={20} className="text-[var(--color-accent)]" aria-hidden="true" /></div><div className="mt-4 flex gap-2"><Filter size={16} className="mt-3 text-[var(--color-muted)]" aria-hidden="true" />{[['all', 'All'], ['pending', 'Pending'], ['completed', 'Completed']].map(([value, label]) => <button key={value} type="button" onClick={() => setFilter(value)} className={`min-h-10 rounded-xl px-3 text-xs font-bold ${filter === value ? 'bg-[var(--color-accent)] text-white' : 'bg-[#f2f7f3] text-[var(--color-muted)]'}`}>{label}</button>)}</div><ul className="mt-4 divide-y divide-[#edf1ed]">{visible.map((habit) => <DailyHabitCard key={habit.id} habit={habit} goalName={goalNames[habit.goalId]} onToggle={onToggle} />)}</ul></section> }