export function WeeklyTrend({ values, period }) { const points = values.map((value, index) => `${index * 16.66},${100 - value}`).join(' '); return <section className="dashboard-card bg-white p-5 sm:p-6" aria-labelledby="weekly-trend-title"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-[var(--color-accent)]">{period === 'weekly' ? 'Last 7 days' : 'Monthly snapshot'}</p><h2 id="weekly-trend-title" className="mt-1 text-xl font-semibold text-[var(--color-ink)]">Weekly Trend</h2></div><span className="text-xs font-bold text-[var(--color-accent)]">{values.at(-1)} today</span></div><svg className="mt-5 h-28 w-full overflow-visible" viewBox="0 0 100 100" preserveAspectRatio="none" role="img" aria-label={`Study health trend: ${values.join(', ')}`}><path d="M 0,100 L 0,0 M 0,100 L 100,100" stroke="#e2ece4" strokeWidth="1" fill="none" /><polyline points={points} fill="none" stroke="#277c68" strokeWidth="2.5" vectorEffect="non-scaling-stroke" /><polyline points={`0,${100 - values[0]} ${points}`} fill="rgba(39,124,104,.08)" stroke="none" /></svg><div className="mt-2 flex justify-between text-[11px] font-semibold text-[var(--color-muted)]">{['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'].map((day) => <span key={day}>{day}</span>)}</div></section> }