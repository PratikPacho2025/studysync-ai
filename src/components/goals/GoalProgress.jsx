export function GoalProgress({ goal }) { const progress = Math.min(100, Math.round((goal.current / goal.target) * 100)); return <div><div className="mb-2 flex justify-between text-sm font-semibold"><span className="text-[var(--color-muted)]">Progress</span><span className="text-[var(--color-ink)]">{progress}%</span></div><div className="h-2 overflow-hidden rounded-full bg-[#e7efe9]"><div className="h-full rounded-full bg-[var(--color-accent)] transition-all" style={{ width: `${progress}%` }} /></div><p className="mt-2 text-xs font-semibold text-[var(--color-muted)]">{goal.current} / {goal.target} {goal.unit}</p></div> }