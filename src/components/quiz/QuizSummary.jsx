export function QuizSummary({ summary }) { return <section className="dashboard-card bg-white p-5 sm:p-6" aria-labelledby="quiz-summary-title"><h2 id="quiz-summary-title" className="text-xl font-semibold text-[var(--color-ink)]">Quiz Summary</h2><div className="mt-5 grid grid-cols-2 gap-3">{[['Quizzes', summary.completed], ['Average score', `${summary.average}%`], ['Accuracy', `${summary.accuracy}%`], ['Weak topics', summary.weak]].map(([label, value]) => <div key={label} className="rounded-2xl bg-[#f2f7f3] p-3"><strong className="block text-2xl font-semibold text-[var(--color-ink)]">{value}</strong><span className="mt-1 block text-xs font-semibold text-[var(--color-muted)]">{label}</span></div>)}</div></section> }