import { CalendarClock, Play } from 'lucide-react'
import { Link } from 'react-router-dom'

export function DelayedTaskCard({ task }) { const formatDate = (date) => new Intl.DateTimeFormat('en-US', { day: 'numeric', month: 'short', hour: 'numeric', minute: '2-digit' }).format(new Date(date)); return <article className="dashboard-card 3d-hover bg-white p-4 sm:p-5"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="text-xs font-bold uppercase tracking-[0.15em] text-[#a87525]">{task.delayedCount} delays</p><h3 className="mt-2 truncate text-lg font-semibold text-[var(--color-ink)]">{task.title}</h3><p className="mt-1 truncate text-sm text-[var(--color-muted)]">{task.topic}</p></div><span className="rounded-full bg-[#f7efd9] px-2.5 py-1 text-[10px] font-bold text-[#9b7024]">Postponed</span></div><div className="mt-4 space-y-2 text-xs text-[var(--color-muted)]"><p className="flex items-center gap-2"><CalendarClock size={13} aria-hidden="true" /> Original: {formatDate(task.originalDate)}</p><p className="flex items-center gap-2"><CalendarClock size={13} aria-hidden="true" /> Last postponed: {formatDate(task.lastDelayedDate)}</p></div><Link to="/study" className="mt-4 flex min-h-11 items-center justify-center gap-2 rounded-xl bg-[#e1f0e7] text-sm font-bold text-[var(--color-accent)] hover:bg-[#d4eadd]"><Play size={16} fill="currentColor" aria-hidden="true" /> Start Now</Link></article> }