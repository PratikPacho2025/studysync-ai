import { CalendarDays, Check, X } from 'lucide-react'
import { useState } from 'react'
import { attendanceLectures } from '../../data/mock/attendance'

export function MarkAttendanceSheet({ isOpen, subjects, onClose, onSave }) {
  const [form, setForm] = useState({ subject: subjects[0]?.subject ?? '', date: new Date().toISOString().slice(0, 10), status: 'present' })
  if (!isOpen) return null
  const selectedLecture = attendanceLectures.find((item) => item.subject === form.subject)
  const update = (event) => setForm((current) => ({ ...current, [event.target.name]: event.target.value }))
  const submit = (event) => { event.preventDefault(); onSave({ ...form, lecture: selectedLecture?.lecture ?? form.subject }); setForm((current) => ({ ...current, status: 'present' })) }
  return <div className="fixed inset-0 z-50 flex items-end justify-center bg-[#173e32]/35 p-0 sm:items-center sm:p-5"><div className="max-h-[92vh] w-full overflow-y-auto rounded-t-3xl bg-[#f8fbf8] p-5 shadow-2xl sm:max-w-lg sm:rounded-3xl sm:p-7"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-[var(--color-accent)]">Manual update</p><h2 className="mt-1 text-2xl font-semibold text-[var(--color-ink)]">Mark Attendance</h2></div><button type="button" onClick={onClose} aria-label="Close mark attendance form" className="grid size-10 place-items-center rounded-xl text-[var(--color-muted)] hover:bg-white"><X size={19} aria-hidden="true" /></button></div><form onSubmit={submit} className="mt-6 space-y-4"><label className="block text-sm font-semibold text-[var(--color-ink)]">Subject<select name="subject" value={form.subject} onChange={update} className="mt-2 min-h-11 w-full rounded-xl border border-[#d8e5db] bg-[#fbfdfb] px-3 text-sm font-normal outline-none focus:border-[var(--color-accent)]">{subjects.map((subject) => <option key={subject.id} value={subject.subject}>{subject.subject}</option>)}</select></label><div className="rounded-xl bg-[#edf5ef] p-3"><p className="text-xs font-semibold text-[var(--color-muted)]">Lecture</p><p className="mt-1 text-sm font-semibold text-[var(--color-ink)]">{selectedLecture?.lecture}</p></div><label className="block text-sm font-semibold text-[var(--color-ink)]">Date<div className="relative mt-2"><CalendarDays size={16} className="pointer-events-none absolute left-3 top-3 text-[var(--color-muted)]" aria-hidden="true" /><input required type="date" name="date" value={form.date} onChange={update} className="min-h-11 w-full rounded-xl border border-[#d8e5db] bg-[#fbfdfb] pl-10 pr-3 text-sm font-normal outline-none focus:border-[var(--color-accent)]" /></div></label><fieldset><legend className="text-sm font-semibold text-[var(--color-ink)]">Status</legend><div className="mt-2 grid grid-cols-2 gap-3"><label className={`flex min-h-12 cursor-pointer items-center gap-2 rounded-xl border px-3 text-sm font-bold ${form.status === 'present' ? 'border-[var(--color-accent)] bg-[#e1f0e7] text-[var(--color-accent)]' : 'border-[#d8e5db] text-[var(--color-muted)]'}`}><input className="sr-only" type="radio" name="status" value="present" checked={form.status === 'present'} onChange={update} /><Check className="pointer-events-none" size={16} aria-hidden="true" /> Present</label><label className={`flex min-h-12 cursor-pointer items-center gap-2 rounded-xl border px-3 text-sm font-bold ${form.status === 'absent' ? 'border-[#a14d2e] bg-[#f7e3dd] text-[#a14d2e]' : 'border-[#d8e5db] text-[var(--color-muted)]'}`}><input className="sr-only" type="radio" name="status" value="absent" checked={form.status === 'absent'} onChange={update} /><X className="pointer-events-none" size={16} aria-hidden="true" /> Absent</label></div></fieldset><div className="flex gap-3 pt-2"><button type="button" onClick={onClose} className="min-h-11 flex-1 rounded-xl border border-[#d8e5db] text-sm font-bold text-[var(--color-muted)]">Cancel</button><button type="submit" className="min-h-11 flex-1 rounded-xl bg-[var(--color-accent)] text-sm font-bold text-white">Save Attendance</button></div></form></div></div>
}