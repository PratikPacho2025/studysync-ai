import { CheckCircle2, Clock3 } from 'lucide-react'
import { StudyProgress } from './StudyProgress'

export function TodayStudySummary({ progress, studyTime }) {
  return <section className="dashboard-card bg-white p-5 sm:p-6" aria-labelledby="today-study-summary-title"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-[var(--color-accent)]">Keep the streak moving</p><h2 id="today-study-summary-title" className="mt-1 text-xl font-semibold text-[var(--color-ink)]">Today&apos;s Progress</h2></div><CheckCircle2 size={21} className="text-[var(--color-accent)]" aria-hidden="true" /></div><div className="mt-5 flex items-end justify-between gap-4"><div><strong className="text-3xl font-semibold text-[var(--color-ink)]">{progress.completed} / {progress.total}</strong><p className="mt-1 text-xs font-semibold text-[var(--color-muted)]">tasks completed</p></div><span className="text-2xl font-semibold text-[var(--color-accent)]">{progress.percentage}%</span></div><div className="mt-4"><StudyProgress percentage={progress.percentage} /></div><div className="mt-5 flex items-center gap-2 border-t border-[#edf1ed] pt-4 text-sm font-semibold text-[var(--color-muted)]"><Clock3 size={16} className="text-[var(--color-accent)]" aria-hidden="true" /> Study time: <span className="text-[var(--color-ink)]">{studyTime.completed} min / {studyTime.planned} min</span></div></section>
}