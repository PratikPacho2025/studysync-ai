export function calculateStudyHealth(data) { return Math.round([data.attendance, data.consistency, data.revision, data.quizPerformance, data.timeManagement, data.goalCompletion].reduce((sum, value) => sum + value, 0) / 6) }
export function getHealthStatus(score) { if (score >= 90) return 'Excellent'; if (score >= 75) return 'Good'; if (score >= 60) return 'Needs Attention'; return 'Critical' }
export function calculateStudyTimeChange(current, previous) { return previous ? Math.round(((current - previous) / previous) * 100) : 0 }
export function getPositiveInsights(data) { const insights = []; if (data.attendance >= 80) insights.push('Your attendance is strong.'); if (data.goalCompletion >= 80) insights.push(`You completed ${data.goalCompletion}% of your planned goals.`); if (data.consistency >= 80) insights.push('Your study rhythm is highly consistent.'); return insights }
export function getWarningInsights(data) { const insights = []; if (data.revision < 70) insights.push('Your revision consistency needs attention.'); if (data.consistency < 70) insights.push('Your active study rhythm dropped recently.'); if (data.quizPerformance < 60) insights.push('Quiz performance suggests some topics need practice.'); return insights }