import { CalendarClock, CheckCircle2 } from 'lucide-react'

export function MissedSessionCard({ sessions }) { return <section className="dashboard-card bg-white p-5 sm:p-6" aria-labelledby="missed-title"><div className="flex items-center gap-2"><CheckCircle2 size={18} className="text-[var(--color-accent)]" aria-hidden="true" /><h2 id="missed-title" className="text-xl font-semibold text-[var(--color-ink)]">Missed Sessions</h2></div><div className="mt-4 divide-y divide-[#edf1ed]">{sessions.map((session) => <div key={session.id} className="flex items-center gap-3 py-3 first:pt-1"><span className="grid size-9 shrink-0 place-items-center rounded-xl bg-[#f7efd9] text-[#a87525]"><CalendarClock size={17} aria-hidden="true" /></span><div><p className="text-sm font-semibold text-[var(--color-ink)]">{session.title}</p><p className="mt-1 text-xs text-[var(--color-muted)]">{session.date} · {session.time}</p></div></div>)}</div></section> }