import { Filter } from 'lucide-react'
import { StudyTaskCard } from './StudyTaskCard'

export function StudyTaskList({ tasks, filter, onFilterChange, onToggle, onSelect, onStart }) {
  return <section aria-labelledby="study-task-list-title"><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-[var(--color-accent)]">The next actions</p><h2 id="study-task-list-title" className="mt-1 text-2xl font-semibold text-[var(--color-ink)]">Today&apos;s Study</h2></div><Filter size={18} className="text-[var(--color-muted)]" aria-hidden="true" /></div><div className="mt-4 flex gap-2 overflow-x-auto pb-1" role="tablist" aria-label="Filter study tasks">{[['all', 'All'], ['pending', 'Pending'], ['completed', 'Completed']].map(([value, label]) => <button key={value} type="button" role="tab" aria-selected={filter === value} onClick={() => onFilterChange(value)} className={`min-h-10 shrink-0 rounded-xl px-3 text-xs font-bold transition ${filter === value ? 'bg-[var(--color-accent)] text-white' : 'bg-white text-[var(--color-muted)] hover:text-[var(--color-accent)]'}`}>{label}</button>)}</div><div className="mt-4 space-y-3">{tasks.length ? tasks.map((task) => <StudyTaskCard key={task.id} task={task} onToggle={onToggle} onSelect={onSelect} onStart={onStart} />) : <p className="dashboard-card bg-white p-8 text-center text-sm text-[var(--color-muted)]">No tasks match this filter.</p>}</div></section>
}