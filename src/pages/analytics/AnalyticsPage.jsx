import { useMemo, useState } from 'react'
import { AnalyticsHeader, AnalyticsInsight, ConsistencyCard, HealthBreakdown, StudyHealthScore, StudyTimeChart, SubjectPerformance, WeakAreas, WeeklyTrend } from '../../components/analytics'
import { analyticsData } from '../../data/mock/analytics'
import { calculateStudyHealth, calculateStudyTimeChange, getHealthStatus, getPositiveInsights, getWarningInsights } from '../../utils/analytics'

export function AnalyticsPage() { const [period, setPeriod] = useState('weekly'); const data = analyticsData[period]; const score = calculateStudyHealth(data); const insights = useMemo(() => ({ positive: getPositiveInsights(data), warning: getWarningInsights(data) }), [data]); return <div className="mx-auto w-full max-w-6xl space-y-7 pb-2"><AnalyticsHeader period={period} onPeriodChange={setPeriod} /><div className="grid gap-5 lg:grid-cols-[minmax(0,0.9fr)_minmax(0,1.1fr)]"><StudyHealthScore score={score} status={getHealthStatus(score)} /><HealthBreakdown data={data} /></div><div className="grid gap-5 lg:grid-cols-2"><WeeklyTrend values={data.trend} period={period} /><StudyTimeChart studyTime={data.studyTime} /></div><div className="grid gap-5 lg:grid-cols-2"><ConsistencyCard data={{ ...data.consistencyDetails, value: data.consistency }} /><SubjectPerformance subjects={data.subjects} /></div><div className="grid gap-5 lg:grid-cols-2"><WeakAreas areas={data.weakAreas} /><AnalyticsInsight positive={insights.positive} warning={insights.warning} /></div><p className="sr-only">Study time changed by {calculateStudyTimeChange(data.studyTime.totalMinutes, data.studyTime.previousWeekMinutes)} percent from the previous period.</p></div>}