import { HealthMetricCard } from './HealthMetricCard'
export function HealthBreakdown({ data }) { const metrics = [['Attendance', data.attendance], ['Consistency', data.consistency], ['Revision', data.revision, 'Down 8%'], ['Quiz Performance', data.quizPerformance], ['Time Management', data.timeManagement], ['Goal Completion', data.goalCompletion]]; return <section aria-labelledby="health-breakdown-title"><div className="mb-4"><p className="text-xs font-bold uppercase tracking-[0.18em] text-[var(--color-accent)]">The six signals</p><h2 id="health-breakdown-title" className="mt-1 text-2xl font-semibold text-[var(--color-ink)]">Health Breakdown</h2></div><div className="grid gap-3 sm:grid-cols-2">{metrics.map(([label, value, trend]) => <HealthMetricCard key={label} label={label} value={value} trend={trend} />)}</div></section> }