import { Check, Play, X } from 'lucide-react'
import { RevisionStatusBadge } from './RevisionStatusBadge'
import { formatRevisionDate } from '../../utils/revisions'

export function RevisionDetailsSheet({ revision, onClose, onStart, onComplete }) {
  if (!revision) return null
  return <div className="fixed inset-0 z-50 flex items-end justify-center bg-[#173e32]/35 p-0 sm:items-center sm:p-5"><div className="w-full rounded-t-3xl bg-[#f8fbf8] p-5 shadow-2xl sm:max-w-lg sm:rounded-3xl sm:p-7"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-[var(--color-accent)]">Revision details</p><h2 className="mt-1 text-2xl font-semibold text-[var(--color-ink)]">{revision.topic}</h2></div><button type="button" onClick={onClose} aria-label="Close revision details" className="grid size-10 place-items-center rounded-xl text-[var(--color-muted)] hover:bg-white"><X size={19} aria-hidden="true" /></button></div><div className="mt-5 flex items-center justify-between"><p className="text-sm font-semibold text-[var(--color-muted)]">{revision.subject} · Revision #{revision.revisionNumber}</p><RevisionStatusBadge status={revision.status} /></div><dl className="mt-5 divide-y divide-[#e2ece4] rounded-2xl bg-white px-4"><div className="flex justify-between gap-4 py-3"><dt className="text-sm text-[var(--color-muted)]">Recommended duration</dt><dd className="text-right text-sm font-semibold text-[var(--color-ink)]">{revision.duration} minutes</dd></div><div className="flex justify-between gap-4 py-3"><dt className="text-sm text-[var(--color-muted)]">Original lecture</dt><dd className="text-right text-sm font-semibold text-[var(--color-ink)]">{formatRevisionDate(revision.lectureDate)} · {formatRevisionDate(revision.lectureDate, { hour: 'numeric', minute: '2-digit' })}</dd></div><div className="flex justify-between gap-4 py-3"><dt className="text-sm text-[var(--color-muted)]">Scheduled revision</dt><dd className="text-right text-sm font-semibold text-[var(--color-ink)]">{formatRevisionDate(revision.scheduledDate)} · {formatRevisionDate(revision.scheduledDate, { hour: 'numeric', minute: '2-digit' })}</dd></div></dl><div className="mt-5 flex gap-3">{revision.status !== 'completed' && <button type="button" onClick={() => onStart(revision)} className="flex min-h-11 flex-1 items-center justify-center gap-2 rounded-xl bg-[#e1f0e7] text-sm font-bold text-[var(--color-accent)]"><Play size={16} fill="currentColor" aria-hidden="true" /> Start Session</button>}<button type="button" onClick={() => onComplete(revision)} className="flex min-h-11 flex-1 items-center justify-center gap-2 rounded-xl bg-[var(--color-accent)] text-sm font-bold text-white"><Check size={16} aria-hidden="true" /> Mark Complete</button></div></div></div>
}