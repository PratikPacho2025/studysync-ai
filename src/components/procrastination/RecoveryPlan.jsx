import { CheckCircle2 } from 'lucide-react'
import { Link } from 'react-router-dom'

export function RecoveryPlan() { const actions = [['Complete DSA revision', '20 minutes'], ["Complete today's CN quiz", '15 minutes'], ['Review one weak topic', '10 minutes']]; return <section className="dashboard-card bg-[#eef5f0] p-5 sm:p-6" aria-labelledby="recovery-title"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-[var(--color-accent)]">A gentle reset</p><h2 id="recovery-title" className="mt-1 text-xl font-semibold text-[var(--color-ink)]">Get Back on Track</h2></div><ol className="mt-5 space-y-4">{actions.map(([title, duration], index) => <li key={title} className="flex gap-3"><span className="grid size-7 shrink-0 place-items-center rounded-full bg-white text-xs font-bold text-[var(--color-accent)]">{index + 1}</span><div><p className="text-sm font-semibold text-[var(--color-ink)]">{title}</p><p className="mt-1 text-xs text-[var(--color-muted)]">{duration}</p></div></li>)}</ol><Link to="/study" className="mt-5 flex min-h-11 items-center justify-center gap-2 rounded-xl bg-white text-sm font-bold text-[var(--color-accent)] hover:bg-[#e1f0e7]"><CheckCircle2 size={16} aria-hidden="true" /> Start a small win</Link></section> }