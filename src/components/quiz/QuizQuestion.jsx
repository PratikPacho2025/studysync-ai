import { AnswerOption } from './AnswerOption'

export function QuizQuestion({ question, selectedAnswer, submitted, onSelect }) { return <section aria-labelledby="question-title"><p className="text-xs font-bold uppercase tracking-[0.16em] text-[var(--color-accent)]">Multiple choice</p><h2 id="question-title" className="mt-3 text-xl font-semibold leading-7 text-[var(--color-ink)]">{question.question}</h2><div className="mt-6 space-y-3">{question.options.map((option) => <AnswerOption key={option} option={option} selected={selectedAnswer === option} submitted={submitted} correct={submitted && option === question.correctAnswer} onSelect={onSelect} />)}</div>{submitted && <p className="mt-5 rounded-xl bg-[#edf5ef] p-3 text-sm leading-5 text-[var(--color-muted)]">{question.explanation}</p>}</section> }